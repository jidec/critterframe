!!! note "For extending the package"
    Documents every public member of `critterframe.records.runs`, not just what `cf.*` re-exports. See the
    [pipeline reference](../index.md) for what a pipeline calls directly.

::: critterframe.records.runs
