"""Antenna light-trap monitoring: API access, export ingest, crop download, and scale calibration.

Antenna (https://api.antenna.insectai.org) detects and crops individual insects
from light-trap sheet images, so its occurrences already satisfy CritterFrame's
one-organism-per-image rule before ingest sees them."""
